language: node_js
matrix:
  include:
    - node_js: "6.0"
sudo: required
dist: trusty
addons:
  apt:
    packages:
    - nginx
    - curl
#before_install:

before_script:
  - sudo cp .travis_nginx.conf /etc/nginx/nginx.conf
  - /etc/init.d/nginx restart
script:
  - echo pwd
  - curl http://localhost/
#after_script:

notifications:
  email: false